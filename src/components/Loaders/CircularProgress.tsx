import { OrbitProgress } from 'react-loading-indicators';

const CircularProgress = () => {
  return <OrbitProgress color='#31b2cc' size='medium' text='' textColor='' />;
};

export default CircularProgress;
